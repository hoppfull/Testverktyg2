<UserControl x:Class="AdminApp.QuestionControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdminApp"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="tbx_QuestionText" />
            <TextBox Grid.Column="1" x:Name="tbx_QuestionScore"
                     Width="50" Text="Poäng" />
            <Button Grid.Column="2" x:Name="btn_QuestionDelete"
                    Content="Ta bort" />
        </Grid>

        <ItemsControl Grid.Row="1" x:Name="icl_Answers" />

        <Button Grid.Row="2" Content="Lägg till svarsalternativ" HorizontalAlignment="Left" />
    </Grid>
</UserControl>
